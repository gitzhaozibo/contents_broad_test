[pytest]
testpaths = tests
python_files = test_*.py
markers =
    unit: fast unit tests with no external dependencies
    integration: API integration tests via FastAPI TestClient (Blob mocked)
    e2e: browser end-to-end tests via Playwright
addopts = -ra
